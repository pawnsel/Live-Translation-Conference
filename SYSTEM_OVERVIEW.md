# สรุปการทำงานของระบบ AI Realtime Conference Interpreter & Translator

ระบบแปลภาษาไทย ⇄ อังกฤษ แบบเรียลไทม์สำหรับงานประชุม ทำงานบนเบราว์เซอร์
ร่วมกับเซิร์ฟเวอร์ Node/Express ตัวเดียวในโปรเจกต์นี้ ใช้ Google Gemini
เป็นเครื่องมือถอดเสียง แปลภาษา และสรุปการประชุม โดยไม่มี backend แยกอีกต่อไป

---

## 1. สถาปัตยกรรมระบบ (System Architecture)

ทั้งระบบมีแค่ **2 process** คือเบราว์เซอร์กับเซิร์ฟเวอร์ Express ตัวเดียว
(`server.ts`) ซึ่งทำหน้าที่ทั้งเสิร์ฟหน้าเว็บ (ผ่าน Vite middleware) และเป็น
ตัวกลางคุยกับ Gemini

### 1.1 เส้นทางคำบรรยายสด (WebSocket — เส้นทางหลัก)

```
ไมโครโฟน
  → AudioWorklet แปลงเป็น PCM 16 kHz mono เฟรมละ 20 ms   (src/asr/audio/pcm.ts)
  → base64 ส่งผ่าน WebSocket ไปยังเซิร์ฟเวอร์ของเราเอง
      ws://<host>/ws/gemini-live-transcribe                (useGeminiLiveCapture.ts)
  → เซิร์ฟเวอร์เปิด WebSocket ของตัวเองไปยัง Gemini        (server/geminiLiveProxy.ts)
      โมเดล gemini-3.5-live-translate-preview
  ← Gemini ส่งกลับเป็นชิ้นข้อความต่อเนื่องระหว่างที่ยังพูดอยู่
      inputTranscription  = ข้อความต้นฉบับ
      outputTranscription = คำแปล
  → hook สะสมชิ้นส่วน แสดงผลสดในกล่อง subtitle (ตัวจาง)
      แล้วปิดเป็น caption หนึ่งรายการเมื่อเงียบครบ 1.2 วินาที
  → captions reducer → UI                                  (src/asr/captions.ts)
```

ก่อนเปิด WebSocket เซิร์ฟเวอร์ตรวจ token ของผู้เรียก (ส่งมาใน
`Sec-WebSocket-Protocol: bearer, <token>`) กับตาราง `access_requests` ก่อนเสมอ
— ถ้าไม่ใช่บัญชีที่อนุมัติแล้ว จะตอบ HTTP 401 ตั้งแต่ตอน handshake
โดยไม่เปิด session กับ Gemini เลย (`server/auth.ts`)

**API key อยู่ฝั่งเซิร์ฟเวอร์เท่านั้น** เบราว์เซอร์ไม่เคยเห็นทั้ง
`GEMINI_API_KEY` และชื่อโมเดล เพราะเชื่อมต่อมาที่เซิร์ฟเวอร์ของเราเสมอ
ไม่ได้ต่อตรงไป Google สิ่งที่ client ส่งขึ้นมาได้มีเพียงคู่ภาษาและรายการ
คำศัพท์จาก glossary ซึ่งเซิร์ฟเวอร์ตรวจสอบ (allowlist + จำกัดจำนวน/ความยาว)
ก่อนใช้งานทุกครั้ง

### 1.2 เส้นทางสรุปการประชุม (HTTP)

```
กด "จบ Session"
  → POST /api/gemini/summarize   (server/geminiRoutes.ts)
  → Gemini รุ่นข้อความ (ค่าเริ่มต้น gemini-3.6-flash)
  → บันทึกสรุปลง project_sessions.summary ใน Postgres (Supabase)
```

ทุก request ต้องแนบ `Authorization: Bearer <token>` และต้องเป็นบัญชีที่
อนุมัติแล้ว มิฉะนั้นได้ HTTP 401/403 ก่อนถึงตัว route

หากเรียก Gemini ไม่สำเร็จหรือเกิน 20 วินาที เซิร์ฟเวอร์ยังตอบ HTTP 200 พร้อม
สรุปว่าง เพื่อรับประกันว่า **transcript ดิบจะไม่หายไปไม่ว่ากรณีใด**

### 1.3 ไฟล์สำคัญ

| ไฟล์ | หน้าที่ |
|---|---|
| `server.ts` | เสิร์ฟหน้าเว็บ + ประกอบ route ทั้งหมด ผูก WebSocket proxy เข้ากับ HTTP server เดียวกัน ค่าเริ่มต้นผูกกับ 127.0.0.1 |
| `server/geminiLiveProxy.ts` | WebSocket relay ไป Gemini, ถือ API key, ตรวจ input จาก client, ตัดเฟรมเสียงที่ไม่ใช้ทิ้ง |
| `server/auth.ts` | ตรวจ token ของผู้เรียกกับตาราง `access_requests` ก่อนให้ใช้ Gemini (ทั้ง REST และ WebSocket) |
| `server/geminiRoutes.ts` | REST endpoint `/api/gemini/summarize` |
| `server/gemini.ts` | เรียก Gemini ผ่าน SDK สำหรับงานที่เป็น HTTP (สรุปผล) |
| `src/asr/audio/useGeminiLiveCapture.ts` | หัวใจฝั่ง client — จับเสียง, สตรีม, สะสมชิ้นข้อความ, ตัดเป็น caption |
| `src/asr/audio/pcm.ts` | นิยามรูปแบบเสียง (16 kHz mono) และ AudioWorklet |
| `src/asr/audio/audioDevices.ts` | กฎการเลือกไมโครโฟน (กรองรายการ, ตั้งชื่อ, fallback เมื่ออุปกรณ์หาย) — pure ทั้งไฟล์ |
| `src/asr/audio/sessionLimits.ts` | กฎว่าเมื่อไหร่ session ต้องปิดตัวเอง (เงียบ 15 นาที / ครบ 6 ชม.) — pure ทั้งไฟล์ |
| `src/data/staleSessions.ts` | กฎการหา session ที่แท็บหายไปแล้ว (heartbeat + reclaim) — pure ทั้งไฟล์ |
| `src/storage/tabSession.ts` | จำ session ของแท็บนี้ใน `sessionStorage` เพื่อปิดทันทีหลัง refresh |
| `src/asr/audio/useAudioInputDevices.ts` | อ่านรายชื่อไมโครโฟนจากเบราว์เซอร์ + ฟัง `devicechange` |
| `src/storage/micStore.ts` | จำไมโครโฟนที่เลือกไว้ต่อเครื่อง (localStorage) |
| `src/storage/safeStorage.ts` | localStorage ที่ไม่ throw — ใช้ร่วมกับ `projectStore.ts` |
| `src/asr/captions.ts` | reducer เก็บ caption ตาม `seq` รองรับการแก้ไขคำแปล |
| `src/glossary.ts` | นิยาม type ของ glossary + แปลงเป็น `customVocabulary` (ไม่แตะ localStorage แล้ว) |
| `src/pages/Admin.tsx` | หน้าจอควบคุมทั้งหมด |
| `src/hooks/useProjects.ts` | โปรเจกต์/ประวัติ session (Supabase) |
| `src/data/projectsRepo.ts` | statement ทั้งหมดต่อ projects/sessions/transcripts บน Supabase |
| `src/data/glossaryRepo.ts` | statement ทั้งหมดต่อ glossary lists/terms/subscriptions บน Supabase |
| `src/data/glossaryImport.ts` | อ่าน/เขียน glossary เป็นไฟล์ JSON (รับทั้ง flat และ sectioned) — pure ทั้งไฟล์ |
| `src/components/billPdfFont.ts` | ฟอนต์ Sarabun (subset, base64) ฝังใน PDF บิล — โหลดแบบ lazy ตอนกดดาวน์โหลดเท่านั้น |
| `src/hooks/useGlossary.ts` | ประกอบ glossary ของแต่ละโปรเจกต์จาก shared lists ที่ subscribe ไว้ + list ของตัวเอง |
| `src/auth/AuthProvider.tsx` | session (Supabase Auth) + สถานะการอนุมัติบัญชี |
| `src/auth/accountStatus.ts` | กฎโดเมนอีเมลที่สมัครได้ + ชนิดข้อมูลสถานะบัญชี |
| `src/pages/Login.tsx` / `Register.tsx` / `AuthCallback.tsx` | เข้าสู่ระบบ, สมัคร (ส่งคำขอ), ปลายทาง OAuth |
| `supabase/schema.sql` | ตาราง `access_requests` + RLS (อนุมัติได้จาก dashboard เท่านั้น) |
| `supabase/schema-projects.sql` | ตาราง projects/project_sessions/transcript_items + glossary lists/terms + RLS ที่ผูกกับ `owner_id = auth.uid()` |

รายละเอียดการติดตั้งฝั่ง Supabase อยู่ใน `docs/supabase-auth-setup.md`
ส่วนงานที่ยังเหลือของระบบ auth อยู่ใน `docs/auth-roadmap.md`

### 1.4 ตัวแปรสภาพแวดล้อม (`.env`)

| ตัวแปร | ค่าเริ่มต้น | ใช้ทำอะไร |
|---|---|---|
| `VITE_SUPABASE_URL` | — (ต้องกำหนด) | URL ของโปรเจกต์ Supabase (อ่านจาก browser) |
| `VITE_SUPABASE_ANON_KEY` | — (ต้องกำหนด) | anon key ของ Supabase — ปลอดภัยเพราะมี RLS กำกับ |
| `VITE_ALLOWED_EMAIL_DOMAIN` | `chula.ac.th` | โดเมนอีเมลที่สมัครสมาชิกได้ (รวมโดเมนย่อย) |
| `GEMINI_API_KEY` | — (ต้องกำหนด) | กุญแจเรียก Gemini ทุกเส้นทาง |
| `GEMINI_LIVE_MODEL` | `gemini-3.5-live-translate-preview` | โมเดลแปลสดผ่าน WebSocket |
| `GEMINI_MODEL` | `gemini-3.6-flash` | โมเดลข้อความสำหรับสรุปผล |
| `GEMINI_SUMMARY_MODEL` | ใช้ `GEMINI_MODEL` | แยกโมเดลสรุปได้ถ้าต้องการ |
| `GEMINI_LIVE_TARGET_LANG` | `en` | ภาษาปลายทางเริ่มต้น (client เลือกทับได้) |
| `GEMINI_LIVE_SOURCE_LANGS` | `th-TH` | ภาษาต้นทางเริ่มต้น |
| `HOST` | `127.0.0.1` | เปลี่ยนเป็น `0.0.0.0` หากต้องเข้าจากเครื่องอื่น |

---

## 2. ฟังก์ชันหลักของระบบ (Core System Features)

### 2.1 กล่อง Subtitle เดียว สำหรับ crop ไป OBS
พื้นที่แสดงคำแปลหลักเป็น **กล่องเดียว** แสดงทีละข้อความ (เหมือน subtitle บน
YouTube) ประวัติทั้งหมด (แก้ไข/คัดลอก/ซ่อน) อยู่ใน panel พับเก็บด้านล่าง

### 2.2 ข้อความไหลสดระหว่างพูด (Live Partial)
ระหว่างที่ยังพูดไม่จบประโยค คำแปลที่ Gemini ทยอยส่งกลับมาจะแสดงในกล่อง
subtitle ทันทีด้วย **ตัวอักษรสีจาง** และแสดงข้อความต้นฉบับสดบนแถบ "กำลังฟัง"
เมื่อผู้พูดหยุด ระบบจะปิดประโยคนั้นเป็น caption หนึ่งรายการ ตัวอักษรเปลี่ยน
เป็นสีเข้ม และรายการถูกบันทึกลงประวัติ (ระหว่างที่ยังเป็นตัวจาง จะยังแก้ไข
ข้อความไม่ได้ เพราะประโยคยังไม่จบ)

### 2.3 การจับคู่ภาษา (Thai ⇄ English เท่านั้น)
เลือกภาษาต้นทางแล้วปลายทางสลับให้อัตโนมัติ รองรับทั้ง ไทย→อังกฤษ และ
อังกฤษ→ไทย การสลับภาษาระหว่าง session จะเชื่อมต่อ WebSocket ใหม่ (เพราะ
ตั้งค่าภาษาได้เฉพาะตอนเปิด session) ใช้เวลาราวหนึ่งวินาที โดยลำดับ caption
เดิมไม่ถูกเขียนทับ

### 2.4 พจนานุกรมศัพท์เฉพาะทาง (Glossary — 3 หมวด, ต่อโปรเจกต์)
glossary เป็นของแต่ละโปรเจกต์ ประกอบจาก shared list ที่แอดมินดูแล ซึ่ง
operator เลือก subscribe ได้จากหน้าโปรเจกต์ บวกกับ list ของโปรเจกต์เอง
เก็บอยู่ในฐานข้อมูล (`src/data/glossaryRepo.ts` + `src/hooks/useGlossary.ts`)
แบ่ง 3 หมวด:
- **ศัพท์เฉพาะ** (`protected_terms`) — ไทย → อังกฤษ
- **ชื่อบุคคล** (`person_names`) — ไทย → อังกฤษ
- **แก้คำไทยที่ฟังผิด** (`thai_corrections`) — ไทย → ไทย

รองรับค้นหา เพิ่ม/ลบทีละคำ และวางสองคอลัมน์จาก Excel/Sheets

**นำเข้า/ส่งออกเป็นไฟล์ `.json`** (`src/data/glossaryImport.ts`) รับสองรูปแบบ
โดยดูจาก top-level key ว่าเป็น object หรือ string:

```jsonc
// flat — ไม่ระบุหมวด จึงลงหมวดที่เปิดอยู่ตอนนั้น (กติกาเดียวกับกล่องวางจาก Excel)
{ "Kawin": "กวิน", "Somchai": "สมชาย" }

// sectioned — ระบุหมวดมาเอง ใช้เก็บทั้ง glossary ได้ในไฟล์เดียว (ปุ่ม Export คายแบบนี้)
{ "en_th_corrections": { "Kawin": "กวิน" },
  "person_names": { "นพ. สมชาย": "Dr. Somchai" } }
```

- ไฟล์ที่ตั้งชื่อหมวดผิดจะถูก**ปฏิเสธพร้อมบอกชื่อหมวดที่ผิด** ไม่ใช่เงียบๆ
  ทิ้งคำทั้งก้อนใต้หมวดนั้น
- กดแล้วยังไม่เขียนทันที — ขึ้นหน้าสรุปว่าจะลงหมวดไหนกี่คำก่อน เพราะ
  **คำที่ซ้ำจะถูกทับด้วยค่าจากไฟล์**
- เตือนคำที่ยาวเกิน 100 ตัวอักษร (เท่ากับ `MAX_TERM_LENGTH` ที่
  `server/geminiLiveBridge.ts` ตัดก่อนส่งให้ Gemini) — ยังนำเข้าให้ แต่คำนั้น
  จะไปถึงโมเดลในรูปที่ถูกตัดสั้น
- **นำเข้าลงคลังของโปรเจกต์นี้เสมอ** แก้ shared list ไม่ได้ (ดู §2.4.2)
- เขียนทั้งไฟล์ด้วย statement เดียว (`glossaryRepo.addTerms`) ไม่ใช่หนึ่งคำ
  ต่อหนึ่ง round trip — กล่องวางจาก Excel ใช้ทางเดียวกันนี้แล้วเช่นกัน

### 2.4.2 การแก้คลังคำศัพท์ที่ใช้ร่วมกัน (shared list)
shared list **แก้จากในแอปไม่ได้ และเป็นเจตนา** — `supabase/schema-projects.sql`
ให้เฉพาะ policy `select` ไม่มี policy เขียนเลย ถ้าแอปลองเขียนจะโดน RLS
ปฏิเสธด้วย error `42501` ไม่ใช่เขียนไม่ผ่านแบบเงียบๆ

แก้ที่ **Supabase Dashboard → SQL Editor** โดยมี cheat-sheet อยู่ท้ายไฟล์
`supabase/schema-projects.sql` แล้ว เช่น เพิ่มชื่อคนเข้าลิสต์
`ชื่อบุคคล (อังกฤษ → ไทย)` ซึ่งเป็นลิสต์ default ที่ทุกโปรเจกต์ subscribe
อัตโนมัติ หน้าจอแอดมินในแอปยังทำไม่ได้ เพราะต้องมี role/`is_admin` ก่อน
(ดู `docs/auth-roadmap.md` §4)

glossary ทำงานสองทางพร้อมกัน ตอนเปิด session:

1. **`customVocabulary`** — ส่งเฉพาะ**ฝั่งภาษาต้นทาง**ของทุกหมวด เพื่อช่วยให้
   ระบบ**รู้จำเสียง**คำเหล่านั้นแม่นขึ้น (ตัวรู้จำเสียงฟังภาษาต้นทาง ไม่ได้ฟัง
   คำแปล) หมวดแก้คำผิดใช้ "คำที่ถูก" ไม่ใช่คำที่ฟังผิด
2. **`systemInstruction`** — ส่งคู่คำของหมวด**ศัพท์เฉพาะ**และ**ชื่อบุคคล**
   เพื่อบังคับ**คำแปล**ให้ตรงตามที่กำหนด (เฉพาะสองหมวดนี้ เพราะหมวดแก้คำผิด
   เป็นไทย→ไทย ไม่เกี่ยวกับการแปล)

> **ข้อจำกัดที่ทดสอบแล้ว:** การบังคับคำแปลเป็นแบบ best-effort — จากการทดสอบ
> ซ้ำหลายรอบ โมเดลทำตามบางคำแต่ไม่ทุกคำเสมอไป โดยเฉพาะคำที่อยู่กลางประโยค
> การเขียนคำสั่งให้เข้มขึ้นไม่ได้ช่วย ถ้าคำไหนต้องถูกต้อง 100% ยังต้องแก้เอง
> ในช่องแก้ไขคำแปล

client ส่งขึ้นมาเป็น**คู่คำที่มีโครงสร้าง** ไม่ใช่ข้อความคำสั่ง — เซิร์ฟเวอร์
เป็นคนประกอบคำสั่งเอง เพื่อไม่ให้เบราว์เซอร์ส่ง prompt อะไรก็ได้เข้าไปใช้
งานบน key ที่มีค่าใช้จ่าย

เนื่องจากตั้งค่าได้เฉพาะตอนเปิด session การแก้ glossary ระหว่าง session จะมี
ผลกับ session ถัดไป

### 2.4.1 เลือกไมโครโฟนได้ (ล็อกระหว่าง session)
ในแถบตั้งค่าด้านข้าง (แท็บภาษา) เหนือช่อง "แสดงประโยคต้นฉบับคู่กับคำแปล"
มีรายการไมโครโฟนให้เลือก ระบบจำค่าไว้ต่อเครื่อง (ไม่ใช่ต่อบัญชี
เพราะ device id ของเครื่องหนึ่งไม่มีความหมายบนอีกเครื่อง) และรีเฟรชรายการเอง
เมื่อมีการเสียบ/ถอดอุปกรณ์ (`devicechange`)

**เปลี่ยนไมโครโฟนระหว่าง session ไม่ได้ แม้ในสถานะพัก** — `deviceId` อยู่ใน
dependency ของ effect ที่สร้าง pipeline เสียง การเปลี่ยนค่าจะรื้อ AudioContext
และเชื่อมต่อ WebSocket ใหม่ ทำให้ประโยคที่ค้างอยู่หาย ตัวเลือกจึงถูก disable
ตั้งแต่ตอนกด "เริ่ม Session" จนกระทั่งการปิด session ทำงานเสร็จ

ชื่อไมโครโฟนจริงจะเห็นได้ก็ต่อเมื่อเคยอนุญาตสิทธิ์ไมโครโฟนแล้วเท่านั้น
(ข้อจำกัดของเบราว์เซอร์) ก่อนหน้านั้นจะแสดงเป็น "ไมโครโฟน 1/2/3" และระบบจะ
อ่านรายชื่อซ้ำอัตโนมัติทันทีที่ session แรกเริ่มฟังสำเร็จ ถ้าอุปกรณ์ที่เลือกไว้
ถูกถอดออกไป จะขึ้นคำเตือนและ session ถัดไปใช้ไมโครโฟนเริ่มต้นแทน

**ถ้าเปิดอุปกรณ์ที่เลือกไม่ได้ ระบบจะถอยไปใช้ไมโครโฟนเริ่มต้นให้เอง** —
`getUserMedia` ที่ระบุ `deviceId: {exact}` จะ throw `OverconstrainedError`
ไม่ใช่หาตัวใกล้เคียงให้ ถ้าปล่อยไว้ประชุมจะเริ่มไม่ได้เลย ระบบจึงลองใหม่อีก
ครั้งโดยไม่ใส่ constraint แล้วขึ้นข้อความสีเหลืองว่ากำลังอัดด้วยไมโครโฟน
เริ่มต้น (ธง `deviceFallback` — ไม่ใช่ error เพราะ session ยังบันทึกอยู่ แต่
ต้องบอก ไม่อย่างนั้นผู้ควบคุมจะเข้าใจว่าไมค์ห้องประชุมทำงานอยู่ทั้งที่เป็น
ไมค์ในตัวเครื่อง) จะลองซ้ำเฉพาะกรณีที่**เลือกอุปกรณ์ไว้**เท่านั้น — ถ้าไม่ได้
เลือกแล้วยังล้มเหลว แปลว่าเป็นปัญหาสิทธิ์ ขอซ้ำก็ได้ผลเหมือนเดิม

### 2.4.3 การปิด session อัตโนมัติ (กันค่าใช้จ่ายวิ่งหนี)
live session คิดเงินตาม wall clock ราว **$2.2/ชั่วโมง** ตราบใดที่ยังเปิดอยู่
และเดิม**ไม่มีอะไรปิดให้เลย** — แท็บที่เปิดค้างเย็นวันศุกร์จะคิดเงินทั้งสุด
สัปดาห์ (~$160) ตอนนี้มีตัวจำกัดสองชั้น:

| เงื่อนไข | ใครตัด | ผล |
|---|---|---|
| ไม่มีเสียงพูด 15 นาที | client | ปิด session แบบปกติ (flush ประโยคสุดท้าย + ปิด record ครบ) |
| ครบ 6 ชั่วโมงต่อ session | client | เหมือนกัน |
| ครบ 6 ชั่วโมง (client ไม่ทำงาน) | server | `close(4408)` — กันแท็บที่ JS ค้าง |
| บัญชีถูกถอนสิทธิ์ | server | `close(4403)` |

**นับจาก "คำพูด" ไม่ใช่ "เฟรมเสียง"** — ไมค์ที่เปิดอยู่ส่งเฟรมตลอดแม้ในห้องเงียบ
เฟรมจึงไม่ได้บอกว่ามีคนพูดอยู่จริงไหม ระบบนับจาก partial หรือ caption ที่ปิดแล้ว
เท่านั้น (`src/asr/audio/sessionLimits.ts` — pure ทั้งไฟล์) ส่วน session ที่กด
**พักค้างไว้ก็นับรวม** เพราะพัก 15 นาทีไม่ใช่การประชุม

client ตัดเองก่อนเสมอเพื่อให้ปิดอย่างถูกต้อง ถ้าตัดจาก server อย่างเดียวฝั่ง
เบราว์เซอร์จะไม่รู้ว่าเกิดอะไรขึ้น — และ `ws.onclose` เดิม**ต่อใหม่ทุกกรณี**
โดยไม่ดู close code ซึ่งจะทำให้การตัดจาก server ไร้ผล (เปิด session ใหม่ทันที
วนไปเรื่อยๆ) จึงเพิ่มรายการ close code ที่ถือเป็น "จบถาวร" ไว้ที่
`TERMINAL_CLOSE_REASONS` — **ต้องแก้คู่กับ `CLOSE_REVOKED`/`CLOSE_MAX_DURATION`
ใน `server/geminiLiveBridge.ts` เสมอ**

### 2.4.3.1 Heartbeat — กัน session ค้างเมื่อแท็บหายไป
auto-stop ในหัวข้อก่อนหน้าอยู่ในแท็บ ถ้าแท็บนั้น**หายไปเอง** (กด refresh,
แท็บ crash, ปิดฝาโน้ตบุ๊ก) ตัวจับเวลาก็หายไปด้วย — `ended_at` ค้างเป็น `null`
ตลอดกาล และเพราะ session ที่ยังเปิดอยู่ถูกคิดเงินถึง `now` บิลจะบวมขึ้นเรื่อยๆ
ทั้งที่ Gemini หยุดคิดเงินไปตั้งแต่วินาทีที่แท็บตาย:

| refresh แล้วทิ้งไว้ | บิลเคยขึ้น | ค่าจริง |
|---|---|---|
| 1 ชั่วโมง | $2.21 | $0 |
| 3 วัน | $158.76 | $0 |

นอกจากเรื่องเงิน session นั้นยังขึ้นป้าย "กำลังดำเนินการ" ทำให้**สรุปไม่ได้**
(ปุ่มสรุปถูก disable เมื่อ `isLive`) และ**สลับโปรเจกต์ไม่ได้**

**วิธีแก้: heartbeat** — แท็บที่กำลังอัดเขียน `project_sessions.last_seen_at`
ทุก 30 วินาที (`SESSION_HEARTBEAT_MS`) ตอนโหลดคอนโซล `reload()` จะปิด session
ที่ heartbeat หยุดไปเกิน 2 นาที (`SESSION_STALE_MS` = 4 จังหวะ) โดย**ปิดที่เวลา
heartbeat สุดท้าย ไม่ใช่เวลาที่มาเจอ** บิลจึงหยุดตรงที่แท็บตายจริงๆ

**การ sweep ทำงานสองจังหวะ** และจำเป็นต้องมีทั้งคู่:

1. **ตอนโหลดคอนโซล** — เก็บกวาด session ที่ตายไปนานแล้ว
2. **ทุก 30 วินาทีหลังจากนั้น** (`STALE_SWEEP_INTERVAL_MS`) — **ขาดข้อนี้ไม่ได้**
   เพราะแท็บที่เพิ่ง refresh ทิ้ง heartbeat ไว้แค่ไม่กี่วินาที ตอน sweep รอบ
   โหลดจึงยัง "สด" อยู่เสมอ ถ้าไม่มีใครมาดูซ้ำ session จะค้าง "กำลังดำเนินการ"
   ตลอดไป — ซึ่งเป็นบั๊กที่เจอตอนทดสอบจริงรอบแรก

**ทางลัดสำหรับเคส refresh: `sessionStorage`** (`src/storage/tabSession.ts`)
แท็บที่กำลังอัดจดหมายเลข session ไว้ใน `sessionStorage` ซึ่ง**รอดจากการ refresh
แต่ตายไปกับแท็บ และไม่แชร์กับแท็บอื่น** พอโหลดใหม่แท็บเดิมจึงรู้ทันทีว่า
"session นี้เคยเป็นของฉัน และตอนนี้ฉันไม่ได้อัดอยู่" → ปิดได้เลยไม่ต้องรอ 2 นาที
ส่วน heartbeat ยังรับผิดชอบเคสที่ไม่มีแท็บเหลือให้รายงาน (crash, ปิดเครื่อง,
คนละเครื่อง) ทั้งสองทางปิดที่ **heartbeat สุดท้าย** เหมือนกัน

กติกาอยู่ใน `src/data/staleSessions.ts` (pure ทั้งไฟล์) จุดที่ต้องระวัง:

- **ยกเว้น session ที่แท็บนี้กำลังอัดอยู่ ด้วย id ไม่ใช่ด้วย heartbeat** —
  คอนโซลเปิดหลายแท็บได้ ถ้าใช้ heartbeat ตัดสิน แท็บที่โหลดหน้าอยู่อาจไปปิด
  ประชุมที่อีกแท็บกำลังอัด หรือปิดของตัวเองระหว่างที่ heartbeat ยังส่งไม่ถึง
- **`closeStaleSession` มี `.is('ended_at', null)` กำกับ** — ถ้าแท็บอื่นปิดไป
  ก่อนแล้ว จะ match 0 แถวแทนที่จะเขียนทับเวลาจบจริงด้วยเวลาที่เก่ากว่า
- **`touchSession` ก็มี `.is('ended_at', null)`** — heartbeat ที่ค้างอยู่ตอน
  operator กดจบ จะได้ไม่ปลุก session ที่ปิดแล้วให้ดูเหมือนยังทำงานอยู่
- **heartbeat เขียนต่อแม้ตอนพัก** — session ที่พักยังเป็นของแท็บนี้ แท็บอื่น
  ต้องไม่ตัดสินว่ามันตายแล้ว
- **หน้าต่าง 2 นาทีกว้างไว้ก่อน** — เบราว์เซอร์ throttle timer ของแท็บที่อยู่
  เบื้องหลัง ถ้าตั้งแคบไปจะปิด session ที่แค่ถูกสลับไปแท็บอื่น
- แถวที่สร้างก่อนมีคอลัมน์นี้จะ fallback ไปใช้ `started_at`

### 2.4.4 ตรวจสิทธิ์ซ้ำระหว่าง session
เดิมตรวจสิทธิ์**ครั้งเดียวตอน handshake** ถอนสิทธิ์กลางประชุมแล้ว session ที่
สตรีมอยู่ยังกินเงินต่อได้เรื่อยๆ ตอนนี้ bridge ตรวจซ้ำทุก 5 นาที
(`REVERIFY_INTERVAL_MS`) และตอบต่างกันตามผลลัพธ์:

| ผล | ทำอะไร | เหตุผล |
|---|---|---|
| `403` ถูกถอนสิทธิ์ | `close(4403)` ทันที | คือเป้าหมายของกลไกนี้ |
| `401` token ใช้ไม่ได้ | เริ่มนับ 10 นาที (`EXPIRED_TOKEN_GRACE_MS`) แล้วค่อยตัด | ดูกับดักด้านล่าง |
| `503` Supabase ล่ม | **ไม่ตัด** | fail-closed ถูกแล้วตอน handshake แต่กลางประชุมคือการฆ่างานจริงเพราะ provider สะดุด |

> **กับดักที่ต้องรู้:** Supabase access token หมดอายุทุก ~1 ชั่วโมง และ
> `createSupabaseVerifier` รายงาน token หมดอายุด้วย **401 ตัวเดียวกับ token
> ปลอม** แยกกันไม่ออก ถ้าตัดทันทีที่เจอ 401 **ทุกการประชุมจะถูกตัดที่นาทีที่ 60**
> ซึ่งคือ use case หลักของระบบ ช่วง grace 10 นาทีจึงมีไว้ให้เบราว์เซอร์ส่ง token
> ใหม่มาทัน — client ยิง `{ authRefresh: { accessToken } }` ผ่าน WebSocket เดิม
> ทุก 4 นาที (`AUTH_REFRESH_INTERVAL_MS`) โดย supabase-js ต่ออายุให้เองอยู่แล้ว
>
> นาฬิกา grace **รีเซ็ตเฉพาะตอน verify ผ่านจริง** ไม่ใช่ตอนได้รับ frame — ไม่
> อย่างนั้น client ที่ถูกถอนสิทธิ์จะยื้อ session ไว้ตลอดกาลด้วยการส่งขยะใหม่
> ทุกไม่กี่นาที

### 2.5 การบันทึกและสรุปช่วงประชุมอัตโนมัติ
ไม่มีปุ่ม "เริ่มบันทึก" — ทุกประโยคที่ปิดแล้วถูกเก็บอัตโนมัติตลอด session
กด "จบ Session" ระบบจะส่ง transcript ทั้งหมดให้ Gemini สรุป **แบบเบื้องหลัง**
ไม่ต้องรอ — ใช้งานต่อได้ทันที ระหว่างที่ยังสรุปไม่เสร็จ panel สรุปและหน้า
ประวัติ session จะขึ้นสถานะ "กำลังสรุป…" ถ้าเรียกไม่สำเร็จจะยังคง transcript
ดิบไว้ครบและขึ้นข้อความเตือนแทนสรุป AI

### 2.6 ประวัติ Session ในโปรเจกต์
กดไอคอน 📋 ข้างชื่อโปรเจกต์เพื่อดู session ทั้งหมดที่เคยบันทึก (เรียงล่าสุด
ก่อน) คลิกเพื่อกางดูสรุป

### 2.7 การส่งออกผลลัพธ์ (Export)
- **TXT**: บทสนทนาพร้อมเวลาและคำแปล
- **SRT**: ไฟล์คำบรรยายพร้อม Timecode

ทั้งสองแบบดึงจาก caption ที่มองเห็นอยู่ (ไม่รวมรายการที่ถูกซ่อน) รวมคำแปลที่
operator แก้เอง ส่วนสรุปด้วย AI และบันทึกโปรเจกต์ถาวรจะรวมรายการที่ถูกซ่อน
ไว้ด้วยเสมอ

---

## 3. วิธีการเริ่มใช้งาน (Quick User Guide)

**เตรียมก่อนใช้งาน**: ตั้งค่า `GEMINI_API_KEY`, `VITE_SUPABASE_URL` และ
`VITE_SUPABASE_ANON_KEY` ใน `.env` (ดูตัวอย่างใน `.env.example`) — ถ้าไม่ตั้ง
ค่า Supabase เซิร์ฟเวอร์จะปฏิเสธทุก request ที่ใช้ Gemini

```bash
npm install
npm run dev      # เสิร์ฟทั้งหน้าเว็บและ API ที่ http://127.0.0.1:3000
```

1. **เลือกหรือสร้างโปรเจกต์** จากหน้าแรก
2. **กด "เริ่ม Session"** มุมขวาบน — ระบบขอสิทธิ์ไมโครโฟน
3. **พูดใส่ไมโครโฟน** — คำแปลจะไหลขึ้นแบบตัวจางทันทีระหว่างพูด และกลายเป็น
   ตัวเข้มเมื่อจบประโยค
4. **สลับภาษา / พัก-เล่นต่อ** ได้จากปุ่มในแถบหัวเรื่องหรือ sidebar
5. **กด "จบ Session"** — ระบบเก็บท้ายประโยคสุดท้ายราว 2-3 วินาทีแล้วคืน
   หน้าจอให้ทันที ส่วนการสรุปทำเบื้องหลัง ระหว่างนั้นจะขึ้น "กำลังสรุป…"
6. ดูสรุปย้อนหลังได้จากไอคอน 📋 ข้างชื่อโปรเจกต์

---

## 4. ข้อจำกัดและพฤติกรรมที่ควรทราบ

- **session แยกตามแท็บ** — ไม่มี session registry กลาง แต่ละแท็บที่กด
  "เริ่ม Session" จับเสียงของตัวเอง
- **แก้ไขคำแปลได้เฉพาะประโยคที่ปิดแล้ว** — ระหว่างที่ยังเป็นตัวจางแก้ไม่ได้
- **สลับภาษาต้องเชื่อมต่อใหม่** — มีช่วงสะดุดสั้นๆ ราวหนึ่งวินาที
- **glossary มีผลตอนเปิด session** — แก้ระหว่าง session จะมีผลรอบถัดไป
- **จบ session ต้องรอสักครู่** — ระบบป้อนความเงียบให้โมเดลราว 1.5 วินาที
  เพื่อให้ปิดประโยคสุดท้ายได้ครบ ไม่เช่นนั้นท้ายประโยคจะขาดหาย ส่วนการสรุป
  ทำเบื้องหลัง ไม่ต้องรอ
- **session ปิดตัวเองเมื่อเงียบ 15 นาที หรือครบ 6 ชั่วโมง** (ดู §2.4.3) —
  ตั้งใจให้เป็นตัวกันค่าใช้จ่าย ไม่ใช่นโยบายการใช้งาน
- **refresh ระหว่าง session ไม่ทำให้ transcript หาย** — caption ถูกเขียนลง
  ฐานข้อมูลทันทีที่ปิดประโยค เสียแค่ประโยคที่ค้างอยู่ ส่วน session ที่ค้างเปิด
  จะถูกปิดย้อนหลังให้ทันทีที่โหลดหน้าใหม่ในแท็บเดิม (ดู §2.4.3.1) ถ้าเป็นแท็บ
  อื่นหรือคนละเครื่อง จะรอ heartbeat ขาดครบ 2 นาทีก่อน
- **session ที่หลุดจะต่อใหม่อัตโนมัติ** — Gemini ปิด session เองเมื่อถึงอายุ
  ที่กำหนด ระบบจะบันทึกประโยคที่ค้างอยู่แล้วเชื่อมต่อใหม่ให้ (สูงสุด 5 ครั้ง
  ติดกันก่อนจะแจ้งให้เริ่ม session ใหม่เอง)
- **caption บันทึกลง database ทันทีที่ปิดประโยค** — ถ้าแท็บหลุดหรือ
  ล่มกลางประชุม จะเสียอย่างมากแค่ประโยคเดียวที่ค้างอยู่ตอนนั้น (ระบบพยายาม
  ส่งซ้ำก่อนจะยอมแพ้ ดู `src/data/captionQueue.ts`) ไม่ใช่ทั้งการประชุม
- **เปลี่ยนไมโครโฟนระหว่าง session ไม่ได้** — ต้องจบ session ก่อน (ดู §2.4.1)
- **PDF บิลต้องใช้ฟอนต์ฝัง** — ฟอนต์ในตัวของ jsPDF (helvetica/times/courier)
  รองรับแค่ Latin-1 ถ้าส่งภาษาไทยเข้าไปมันจะไม่ error แต่จะเขียน UTF-16 ดิบ
  ลงฟอนต์ที่ encode แบบ WinAnsi แล้วออกมาเป็นตัวขยะ ซึ่งโดนตรงจุดที่สำคัญที่สุด
  คือ**ชื่อโปรเจกต์** ที่มักเป็นภาษาไทย ทุก `setFont` ใน `ProjectPanel.tsx` จึง
  ต้องใช้ `PDF_FONT` เท่านั้น **ห้ามเปลี่ยนกลับไปเป็น `'helvetica'`**
  ตัวอักษรที่อยู่นอก subset (Latin, Latin-1, บล็อกไทย, `– — ' ' " " • … → ·`)
  จะกลายเป็นกล่องว่าง ถ้าต้องใช้เพิ่มให้ขยาย subset ตามวิธีที่เขียนไว้หัวไฟล์
  `src/components/billPdfFont.ts` อย่าเพิ่มฟอนต์ตัวที่สอง
- **การอ่าน transcript แบ่งเป็นหน้า** — PostgREST จำกัดจำนวน row ต่อหนึ่ง
  request (Supabase ตั้งไว้ที่ 1000) และ **ไม่แจ้งเมื่อตัดข้อมูล** มันแค่ตอบ
  array สั้นๆ กลับมา ประชุม 3 ชั่วโมงมี caption ราว 1,400–2,200 รายการ
  `projectsRepo.ts` จึงวนอ่านทีละ 1000 จนกว่าจะได้หน้าว่าง โดยเลื่อน offset
  ตาม**จำนวน row ที่ได้จริง** ไม่ใช่ตามขนาดหน้า (เผื่อ cap ฝั่ง server เล็กกว่า
  ที่ขอ) และถือว่า "หน้าสั้น" ยังไม่ใช่จุดจบ — มีเพียงหน้าว่างเท่านั้นที่ใช่
  ส่วนพื้นที่เก็บไม่ใช่ปัญหา: ประชุม 3 ชั่วโมงใช้ราว 1 MB

---

## 5. แผนพัฒนาต่อ (Roadmap)

- ระบบยืนยันตัวตนสำหรับการใช้งานข้ามเครื่อง
- รองรับคู่ภาษาเพิ่มเติมนอกเหนือจากไทย ⇄ อังกฤษ
- ทำให้ glossary มีผลทันทีโดยไม่ต้องเริ่ม session ใหม่
- ตั้ง `pg_cron` ให้กวาดล้างโปรเจกต์ที่หมดอายุ 7 วันในฐานข้อมูลเอง แทนที่จะ
  พึ่งตัวจับเวลาฝั่ง client ซึ่งทำงานเฉพาะตอนเปิด console ค้างไว้เท่านั้น
- หน้าจอแอดมินในแอปสำหรับแก้ไข shared glossary list (ต้องรอ role/`is_admin`
  ตามที่ระบุไว้ใน `docs/auth-roadmap.md` §4 ก่อน)
- บังคับกฎ "โปรเจกต์ active ได้สูงสุด 3 อัน" ในฐานข้อมูลด้วย ไม่ใช่แค่ฝั่ง
  client
- ซิงก์ผ่าน Realtime ระหว่างสองแท็บ/อุปกรณ์ที่ล็อกอินบัญชีเดียวกัน
